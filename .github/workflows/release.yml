name: Build and Release

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'version'

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: windows-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26.1'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install Wails CLI
        run: go install github.com/wailsapp/wails/v2/cmd/wails@latest

      - name: Bump Version and Tag
        id: bump_version
        shell: bash
        run: |
          if [ ! -f "version" ]; then
            echo "0.0.0" > version
          fi
          VERSION=$(cat version | tr -d '\r')
          IFS='.' read -r -a parts <<< "$VERSION"
          patch=$((parts[2] + 1))
          NEW_VERSION="${parts[0]}.${parts[1]}.$patch"
          echo "$NEW_VERSION" > version
          echo "NEW_VERSION=$NEW_VERSION" >> $GITHUB_ENV
          
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add version
          git commit -m "chore: bump version to $NEW_VERSION [skip ci]"
          git push
          
          git tag "v$NEW_VERSION"
          git push origin "v$NEW_VERSION"

      - name: Build Probe Controller (Linux amd64)
        shell: bash
        run: |
          cd probe_controller
          GOOS=linux GOARCH=amd64 go build -ldflags "-X github.com/cloudhelper/probe_controller/internal/core.BuildVersion=v${NEW_VERSION}" -o cloudhelper-probe-controller-linux-amd64 main.go

      - name: Build Probe Manager (Windows amd64)
        shell: bash
        run: |
          cd probe_manager
          wails build -platform windows/amd64 -ldflags "-X main.BuildVersion=v${NEW_VERSION}"
          mv build/bin/probe_manager.exe build/bin/cloudhelper-probe-manager-windows-amd64.exe

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: "v${{ env.NEW_VERSION }}"
          name: "Release v${{ env.NEW_VERSION }}"
          files: |
            probe_controller/cloudhelper-probe-controller-linux-amd64
            probe_manager/build/bin/cloudhelper-probe-manager-windows-amd64.exe
