# 需求跟踪表 `probe_node` 本机 DNS 服务独立启用

## 工作依据与规则传递声明
- 当前角色: 架构师
- 工作依据文档: `doc/ai-coding-unified-rules.md`
- 适用规则: AI协作统一规则 单一规范
- 规则遵循声明: 必须遵守本规则。
- 协作传递要求: 后续接手者与协作者必须遵守同一规则，不得降级或替换执行口径。

- 日期: 2026-04-25
- 备注: 对应新增本机 DNS 服务需求，不依赖 TUN，默认启用，监听 `127.0.0.1`，端口策略 `53` 优先 `5353` 回退，页面提示状态；远方 DNS 来源优先级固定为 `doh_proxy_servers` > `doh_servers` > `dot_servers` > `dns_servers`，且 TUN 不再单独维护 DoH 上游，统一使用本地 DNS 服务配置；DNS 缓存 TTL 固定为 15 天。
- 风险:
  - 低权限环境对 `53` 监听受限。
  - 系统 DNS 未指向本机回环地址时，功能表现可能与预期有偏差。
- 遗留事项:
  - 本期不包含系统 DNS 自动接管与回滚功能。
- 进度状态: 已完成
- 完成情况: 已完成编码与测试回归，`probe_node` 本机 DNS 服务默认启用与状态可观测能力已落地。
- 检查表:
  - [x] 已建立需求编号
  - [x] 已完成执行单元包映射
  - [x] 已确认字符集编码基线 UTF-8 无 BOM LF
  - [x] 已完成编码实现
  - [x] 已完成测试回归
- 跟踪表状态: 已实现
- 结论记录: 已实现独立 DNS 组件方案，默认启用本机回环监听，端口失败回退，状态接口与页面提示可观测。

## 字符集编码基线
- 字符集类型: UTF-8
- BOM 策略: 无 BOM
- 换行符规则: LF
- 跨平台兼容要求: 本次新增与改造文件统一按该基线执行。
- 历史文件迁移策略: 不做全量迁移，按本次改动范围执行。

## 需求跟踪表
| 需求编号 | 需求描述 | 执行单元包 | 编码状态 | 测试状态 | 当前责任角色 | 风险与遗留 | 最新更新时间 |
|---|---|---|---|---|---|---|---|
| RQ-PN-DNS-SVC-001 | 新增本机 DNS 服务且不依赖 TUN | PKG-PN-DNS-SVC-01 | 已实现 | 已通过 | 编码工程师 | 生命周期与 TUN 解耦 | 2026-04-25 |
| RQ-PN-DNS-SVC-002 | DNS 服务默认启用 | PKG-PN-DNS-SVC-02 | 已实现 | 已通过 | 编码工程师 | 启动失败需可观测 | 2026-04-25 |
| RQ-PN-DNS-SVC-003 | 监听地址固定 `127.0.0.1` | PKG-PN-DNS-SVC-03 | 已实现 | 已通过 | 编码工程师 | 地址固定策略需文档一致 | 2026-04-25 |
| RQ-PN-DNS-SVC-004 | 端口优先 `53` 失败回退 `5353` | PKG-PN-DNS-SVC-03 | 已实现 | 已通过 | 编码工程师 | 回退判定与错误信息一致性 | 2026-04-25 |
| RQ-PN-DNS-SVC-005 | 增加 DNS 状态接口 | PKG-PN-DNS-SVC-05 | 已实现 | 已通过 | 编码工程师 | 状态字段稳定性 | 2026-04-25 |
| RQ-PN-DNS-SVC-006 | 本地控制台显示 DNS 状态提示 | PKG-PN-DNS-SVC-05 | 已实现 | 已通过 | 编码工程师 | 页面与接口状态一致 | 2026-04-25 |
| RQ-PN-DNS-SVC-007 | 接入 `proxy_group` 顶层 DNS 上游字段含 `doh_proxy_servers` | PKG-PN-DNS-SVC-04 | 已实现 | 已通过 | 编码工程师 | 字段校验与兼容读写 | 2026-04-25 |
| RQ-PN-DNS-SVC-008 | 远方 DNS 来源优先级固定为 `doh_proxy_servers` > `doh_servers` > `dot_servers` > `dns_servers` | PKG-PN-DNS-SVC-04 | 已实现 | 已通过 | 编码工程师 | 需确保降级顺序稳定 可观测 | 2026-04-25 |
| RQ-PN-DNS-SVC-009 | DNS 缓存 TTL 固定为 15 天 | PKG-PN-DNS-SVC-01 PKG-PN-DNS-SVC-04 | 已实现 | 已通过 | 编码工程师 | TTL 需要在状态与日志中可观测 | 2026-04-25 |

## 执行单元包
- PKG-PN-DNS-SVC-01: DNS 服务组件与状态模型
- PKG-PN-DNS-SVC-02: 启动默认启用与停止回收
- PKG-PN-DNS-SVC-03: 监听端口优先与回退策略
- PKG-PN-DNS-SVC-04: `proxy_group` 顶层 DNS 字段接入与校验
- PKG-PN-DNS-SVC-05: 状态接口与前端提示
- PKG-PN-DNS-SVC-06: 回归测试与方法守卫

## 门禁记录
- G1 需求门: 通过
- G2 架构门: 通过
- G3 编码核查门: 通过
- G4 测试核查门: 通过
- G5 复盘门: 待执行

## 处置动作
- 已完成 `PKG-PN-DNS-SVC-01` 到 `PKG-PN-DNS-SVC-03`，基础服务可运行且默认启用。
- 已完成 `PKG-PN-DNS-SVC-04` 到 `PKG-PN-DNS-SVC-06`，状态接口 页面提示与测试已回归通过。
